{
  "name": "@superstruct-user/jwks-api",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone JWKS endpoint — the token issuer for API Gateway's native JWT authorizer.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "package": "osls package",
    "deploy": "osls deploy",
    "remove": "osls remove"
  },
  "dependencies": {
    "jose": "^6.2.9"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "osls": "^4.1.0",
    "serverless-esbuild": "^1.57.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.11"
  }
}
